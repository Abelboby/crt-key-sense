import { Layout } from "@/components/Layout";
import { SemanticIntent } from "@/components/SemanticIntent";

const Intent = () => {
  return (
    <Layout>
      <SemanticIntent />
    </Layout>
  );
};

export default Intent;