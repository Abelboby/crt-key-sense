import { Layout } from "@/components/Layout";
import { KeyManagement } from "@/components/KeyManagement";

const Keys = () => {
  return (
    <Layout>
      <KeyManagement />
    </Layout>
  );
};

export default Keys;